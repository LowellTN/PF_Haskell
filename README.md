# PF_Haskell

Projet de programmation fonctionnel en Haskell sur la compression de fichiers.

### Utilisation

Pour exécuter un fichier (à la racine du projet):

```bash
cabal build Main.hs
cabal run Main.hs
```